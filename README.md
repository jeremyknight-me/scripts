# Scripts

This repo is a personal library of scripts for routine tasks (log parsing, data
loading, maintenance, snippets). It is a collection of small scripts and utilities I use or have used in the past. Files are grouped by technology under top-level
folders like `powershell/`, `sql/`, `linqpad/`, and `snippets/`.
